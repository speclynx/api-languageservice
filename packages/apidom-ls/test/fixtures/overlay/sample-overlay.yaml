overlay: "1.1.0"
info:
  title: Overlay to add documentation links
  version: "1.0.0"
extends: https://example.com/openapi.yaml
actions:
  - target: "$.paths['/users'].get"
    description: Add description to GET /users
    update:
      description: Returns a list of users
  - target: "$.paths['/users'].post"
    remove: true
